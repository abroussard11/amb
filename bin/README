This is where we build the project.

Directions to build the project:
   cd bin/
   cmake ../
   make
   
Note: You should only have to run cmake ../ the first time. You only have to run "make" each time you
whish to compile after cmake has run.